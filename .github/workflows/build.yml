name: Build

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 1 * *"

jobs:
  ubuntu:
    # TODO Use ubuntu-latest
    # https://docs.github.com/en/free-pro-team@latest/actions/reference/specifications-for-github-hosted-runners
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        build_type:
          - { name: "Release" }
          - { name: "Debug" }
        config:
          - { cc: "gcc-10", cxx: "g++-10" }
          - { cc: "clang-10", cxx: "clang++-10" }

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          sudo add-apt-repository ppa:ubuntu-toolchain-r/ppa
          sudo apt update
          sudo apt install gcc-10 g++-10
          wget -q https://github.com/google/googletest/archive/release-1.10.0.zip
          unzip -q release-1.10.0.zip
          cd googletest-release-1.10.0
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_GMOCK=OFF -DBUILD_SHARED_LIBS=ON
          cmake --build build --config Release -j$(nproc)
          sudo cmake --build build --config Release --target install
          sudo ldconfig
        env:
          CC: gcc-10
          CXX: g++-10

      - name: Configure
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type.name }} \
          -DKLIB_BUILD_TESTS=ON
        env:
          CC: ${{ matrix.config.cc }}
          CXX: ${{ matrix.config.cxx }}

      - name: Build
        run: cmake --build build --config ${{ matrix.build_type.name }} -j$(nproc)

      - name: Test
        run: cd build ; ctest -C ${{ matrix.build_type.name }} -j$(nproc) --output-on-failure

  macos:
    runs-on: macos-latest
    strategy:
      matrix:
        build_type:
          - { name: "Release" }
          - { name: "Debug" }

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          wget -q https://github.com/google/googletest/archive/release-1.10.0.zip
          unzip -q release-1.10.0.zip
          cd googletest-release-1.10.0
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_GMOCK=OFF -DBUILD_SHARED_LIBS=ON
          cmake --build build --config Release -j`sysctl -n hw.ncpu`
          sudo cmake --build build --config Release --target install
          sudo update_dyld_shared_cache

      - name: Configure
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type.name }} \
          -DKLIB_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build --config ${{ matrix.build_type.name }} -j`sysctl -n hw.ncpu`
        env:
          C_INCLUDE_PATH: /usr/local/include
          CPLUS_INCLUDE_PATH: /usr/local/include

      - name: Test
        run: |
          cd build ; ctest -C ${{ matrix.build_type.name }} -j`sysctl -n hw.ncpu` \
          --output-on-failure
