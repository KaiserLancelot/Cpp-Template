name: CodeQL-Config

queries:
  - uses: security-and-quality
# TODO The "paths"/"paths-ignore" fields of the config
# only have effect for Javascript and Python
# https://github.com/github/codeql-action/blob/main/src/analysis-paths.ts
